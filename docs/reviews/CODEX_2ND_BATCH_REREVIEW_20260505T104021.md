**Findings**

P0: 指摘なし。

P1: 指摘なし。前回 P1 は closure できています。`main()` の return は `emit_json(status, exit_code, **extra)` 経由に集約され、`emit_json` が `status`/`exit_code` を payload に入れます ([voicevox_narration.py](/Users/rokumasuda/.claude/plugins/supermovie/template/scripts/voicevox_narration.py:533))。`rg` 実測でも `return emit_json(...)` は 15 件、raw return は見当たりませんでした（Bash 実測）。`no_chunks_succeeded`、`partial_chunks_disallowed`、`write_narration_data_wave_error`、`concat_fail`、`sentinel_write_fail`、`vad_invalid` も status 名として明示されています ([voicevox_narration.py](/Users/rokumasuda/.claude/plugins/supermovie/template/scripts/voicevox_narration.py:608), [voicevox_narration.py](/Users/rokumasuda/.claude/plugins/supermovie/template/scripts/voicevox_narration.py:630), [voicevox_narration.py](/Users/rokumasuda/.claude/plugins/supermovie/template/scripts/voicevox_narration.py:644), [voicevox_narration.py](/Users/rokumasuda/.claude/plugins/supermovie/template/scripts/voicevox_narration.py:677), [voicevox_narration.py](/Users/rokumasuda/.claude/plugins/supermovie/template/scripts/voicevox_narration.py:715), [voicevox_narration.py](/Users/rokumasuda/.claude/plugins/supermovie/template/scripts/voicevox_narration.py:761))。

P2: 1 件。6 gate のうち worktree clean gate が未達です。`check_release_ready.sh` は untracked file があると exit 3 で止まる実装で ([check_release_ready.sh](/Users/rokumasuda/.claude/plugins/supermovie/scripts/check_release_ready.sh:51))、実測では `docs/reviews/CODEX_2ND_BATCH_REREVIEW_20260505T104021.md` が untracked として検出され、script は exit 3 でした（Bash 実測）。

P3: 指摘なし。

P2 fix は両方 closure。`PHASE3_PROGRESS.md` は 35 test と説明しています ([PHASE3_PROGRESS.md](/Users/rokumasuda/.claude/plugins/supermovie/docs/PHASE3_PROGRESS.md:239))、harness の main list も 35 件です（Bash `awk` 実測、[test_timeline_integration.py](/Users/rokumasuda/.claude/plugins/supermovie/template/scripts/test_timeline_integration.py:1886)）。visual smoke fixture は `PROJ`/`VIDEO_CONFIG`/`MAIN_VIDEO`/`REMOTION_BIN`/`render_still`/`probe_dim`/`has_drawtext_filter`/`shutil.which` を mock/restore し、exit 2、restore、`mismatched=1`/`total=1` を assert しています ([test_timeline_integration.py](/Users/rokumasuda/.claude/plugins/supermovie/template/scripts/test_timeline_integration.py:1761), [test_timeline_integration.py](/Users/rokumasuda/.claude/plugins/supermovie/template/scripts/test_timeline_integration.py:1797), [test_timeline_integration.py](/Users/rokumasuda/.claude/plugins/supermovie/template/scripts/test_timeline_integration.py:1807))。

Gate 補足: regen verify は drift 1 で pass、lint は exit 0（Bash 実測）。Python/React は read-only sandbox の temp/cache 書込 EPERM で未証明です（Bash 実測）。
