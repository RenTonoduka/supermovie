**Findings**

- **P1** — [template/scripts/voicevox_narration.py](/Users/rokumasuda/.claude/plugins/supermovie/template/scripts/voicevox_narration.py:509), [template/scripts/voicevox_narration.py](/Users/rokumasuda/.claude/plugins/supermovie/template/scripts/voicevox_narration.py:671), [template/scripts/voicevox_narration.py](/Users/rokumasuda/.claude/plugins/supermovie/template/scripts/voicevox_narration.py:712)  
  詳細: `--output` で custom wav を指定でき、concat は `out_path` に書きますが、sentinel write failure rollback は hard-coded `NARRATION_LEGACY_WAV` だけを unlink します。`--output public/foo.wav` などでは ready 失敗後に wav が残り、design §3 の all-or-nothing 契約に反します（[design](/Users/rokumasuda/.claude/plugins/supermovie/docs/reviews/CODEX_P5_VOICEVOX_SENTINEL_DESIGN_20260505T095934.md:37)）。  
  修正案: rollback 対象を `out_path` にする。default 以外を publish 対象外にするなら `--output` 使用時は sentinel 無効化/エラー化。Effort: S

- **P2** — [template/src/Narration/useNarrationMode.ts](/Users/rokumasuda/.claude/plugins/supermovie/template/src/Narration/useNarrationMode.ts:62), [template/src/Narration/useNarrationMode.ts](/Users/rokumasuda/.claude/plugins/supermovie/template/src/Narration/useNarrationMode.ts:81)  
  詳細: design §2 は近接する legacy/chunk/sentinel event を `scheduleUpdate()` で 1 pending に集約するとしていますが、実装は各 callback で即 `invalidateNarrationMode()` + `setMode()` しています（[design](/Users/rokumasuda/.claude/plugins/supermovie/docs/reviews/CODEX_P5_VOICEVOX_SENTINEL_DESIGN_20260505T095934.md:29)）。dedup は per-file 同一 metadata だけで、cross-file burst は潰せません。  
  修正案: `pendingRef` を追加し、`queueMicrotask` か `requestAnimationFrame` で invalidate/setMode を 1 回に集約。Effort: S

- **P2** — [template/src/Narration/useNarrationMode.chunks.test.tsx](/Users/rokumasuda/.claude/plugins/supermovie/template/src/Narration/useNarrationMode.chunks.test.tsx:20), [template/src/Narration/useNarrationMode.chunks.test.tsx](/Users/rokumasuda/.claude/plugins/supermovie/template/src/Narration/useNarrationMode.chunks.test.tsx:37)  
  詳細: test mock callback が `() => void` で、`__triggerWatch()` も引数なしです。実装の sentinel null guard と `lastModified:sizeInBytes` dedup は metadata/null が来た時だけ動くため、現 test では主仕様を検証できません（[useNarrationMode.ts](/Users/rokumasuda/.claude/plugins/supermovie/template/src/Narration/useNarrationMode.ts:66), [useNarrationMode.ts](/Users/rokumasuda/.claude/plugins/supermovie/template/src/Narration/useNarrationMode.ts:71)）。  
  修正案: mock を `(file?: StaticFileMeta)` 対応にし、`null` 無視と同一 metadata no-op を assertion。Effort: S

- **P2** — [template/scripts/test_timeline_integration.py](/Users/rokumasuda/.claude/plugins/supermovie/template/scripts/test_timeline_integration.py:1352)  
  詳細: rollback test は concat fail のみで、design §4 が求める sentinel write failure 後の wav/chunks/meta/data rollback を踏んでいません（[design](/Users/rokumasuda/.claude/plugins/supermovie/docs/reviews/CODEX_P5_VOICEVOX_SENTINEL_DESIGN_20260505T095934.md:43)）。  
  修正案: `write_narration_ready` を raise する mock test を追加し、default/custom `out_path` 両方を検証。Effort: M

**Gate メモ**: lint は exit 0（Bash実測）。Python/React test は read-only sandbox の temp/cache EPERM で実行検証不能でした（Bash実測）。
